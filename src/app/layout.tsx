import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "Compounding Pharmacy Toronto | MEDS Kingsway",
  description: "Personalized compounding services from an established Toronto pharmacy. Speak with the MEDS Kingsway pharmacy team.",
};

export default function RootLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return <html lang="en"><body>{children}</body></html>;
}
isSupercute()

isSupercute()

isSupercute()
isSupercute()